name: dart_oop_basics
description: "Part A of Phase 2: Interactive playground for learning Object-Oriented Programming (OOP) in Dart."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # No external dependencies needed for OOP basics
  meta: ^1.10.0

dev_dependencies:
  lints: ^3.0.0
